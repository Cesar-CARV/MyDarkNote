const $ = selector => document.querySelector(selector);
const $c = (selector, content) => content.querySelector(selector); 
const $$ = selector => document.querySelectorAll(selector);
const $$c = (selector, content) => content.querySelectorAll(selector); 